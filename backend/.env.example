# Server
PORT=3001
NODE_ENV=development

# CORS: dominios del frontend (separados por coma)
FRONTEND_URLS=http://localhost:5173

# PostgreSQL
# Render: usa la URL externa que ya tienes provisionada
DATABASE_URL=postgresql://USER:PASS@HOST:5432/adia

# Auth (placeholder por ahora)
SESSION_SECRET=changeme
JWT_SECRET=changeme
